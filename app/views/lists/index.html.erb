<h1>一覧ページ</h1>
<% @lists.each do |list| %>
  <div>
    <%= link_to list.title, list_path(list.id) %>
    <%= list.body %>
  </div>
  <p>画像</p>
  <%= image_tag list.image, size: "200x200" %>
<% end %>
<%= link_to "新規投稿", new_list_path %>